# This file lists project requirements that should be installed into the
# local virtualenv library. It includes libraries not needed in GAE (either
# because they're for testing or because GAE already has them).

pytest
